import { environment } from 'src/environments/environment';

export const [BASE_URL, INITIAL_LOADING_TIME, DATA_CLEANSING_KEY] = [
  environment.baseUrl,
  environment.loadingTime,
  environment.dataCleansingKey
];
